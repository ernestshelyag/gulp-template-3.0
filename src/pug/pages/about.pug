extends ../template

block variables
    - var title = 'About'
    - var bodyFlex = true

block content

    include ../common/header

    +b.MAIN.about(class=bodyFlex ? 'main-flex' : '')
        +e.SPAN.text About text

    include ../common/footer
